---
import Layout from '../../layouts/Layout.astro';
import HeaderSup from '../../components/HeaderSup.astro';
import Footer from '../../components/Footer.astro';
import Filtrar from '../../components/Filtrar.jsx';
import Productos from '../../components/Productos.astro';

import dataLaptops from '../../data/dataLaptops.js';
---

<Layout title="Categoría Laptops | Teny">
	<header>
		<HeaderSup />
	</header>
	<main>
		<img class="h-full w-full" src="/assets/images/banners/Banner Laptops.png" alt="Oferta 1"/>
		<section class="flex justify-between py-10">
			<div class="ml-5">
				<Filtrar client:visible setFiltro='categorias'/>
			</div>
			<div class="grid grid-cols-4 gap-4">
				{
					dataLaptops.map((producto, index) => (
						<Productos id={producto.id} nombre={producto.nombre} precio={producto.precio} cantidad={1} src={producto.src} className={index%4 === 0 ? '':'mr-5'}/>
					))
				}
			</div>
		</section>
	</main>
	<footer>
		<Footer />
	</footer>
</Layout>

<style is:global>
    * {
        color: white;
        font-family: 'KoPub Batang';
        font-weight: 400;
        word-wrap: break-word;
    }
	h2 {
		font-family: 'Volkhov';
	}
</style>

<style>
	h2 {
		text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
	}
</style>